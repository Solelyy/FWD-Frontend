export const metadata = {
    title: "Create Account",
    description: "Create employee account to get started in FWD Portal",
}
export default function SignUpLayout({children}: {children:React.ReactNode}) {
   return <>{children}</>
}