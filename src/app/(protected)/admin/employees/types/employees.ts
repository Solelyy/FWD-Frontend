export type EmployeesSummaryResponse = {
    totalAccounts: number;
    totalActive: number;
    totalInactive: number;
    totalPending: number;
    totalExpired: number;
    totalSuspended: number;
}